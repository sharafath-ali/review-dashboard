services:
  postgres:
    image: postgres:16-alpine
    container_name: reviewdash-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: reviewdash
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - INGEST_SECRET=${INGEST_SECRET}
    container_name: reviewdash-app
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/reviewdash
      # Note: inside Docker, app connects to 'postgres' container directly on 5432
      RAINFOREST_API_KEY: ${RAINFOREST_API_KEY}
      INGEST_SECRET: ${INGEST_SECRET}
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    command: sh -c "npx tsx node_modules/knex/bin/cli.js migrate:latest && node .next/standalone/server.js"
    restart: unless-stopped

  db-migrate:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - INGEST_SECRET=${INGEST_SECRET}
    container_name: reviewdash-migration
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/reviewdash
    depends_on:
      postgres:
        condition: service_healthy
    command: npx tsx node_modules/knex/bin/cli.js migrate:latest

volumes:
  postgres_data:
